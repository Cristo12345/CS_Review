// Linked Lists
// A linear data structure. Made up of nodes that aren't necessarily stored next to each other in memory
// Each node contains a value and a pointer to next node. Points to NULL if last node. 

// [ A ] -> [ B ] -> [ C ] -> NULL


    